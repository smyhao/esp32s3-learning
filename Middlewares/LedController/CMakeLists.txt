set(LEDCONTROLLER_SRC_DIRS "LedController")
set(LEDCONTROLLER_INCLUDE_DIRS "LedController")
set(LEDCONTROLLER_REQUIRES BSP esp_timer)

idf_component_register(
    SRC_DIRS ${LEDCONTROLLER_SRC_DIRS}
    INCLUDE_DIRS ${LEDCONTROLLER_INCLUDE_DIRS}
    REQUIRES ${LEDCONTROLLER_REQUIRES}
)
