set(STRIPCONFIG_SRC_DIRS "StripConfig")
set(STRIPCONFIG_INCLUDE_DIRS "StripConfig")
set(STRIPCONFIG_REQUIRES nvs_flash LedController)

idf_component_register(
    SRC_DIRS ${STRIPCONFIG_SRC_DIRS}
    INCLUDE_DIRS ${STRIPCONFIG_INCLUDE_DIRS}
    REQUIRES ${STRIPCONFIG_REQUIRES}
)
